import 'package:flutter/material.dart';
class ThirdQuestionScreen extends StatelessWidget{
  ThirdQuestionScreen({super.key});
  @override
  Widget build(BuildContext context){
    return Scaffold(

    );
  }
}